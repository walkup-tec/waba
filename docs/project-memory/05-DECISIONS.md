- 2026-09-04: relatório de campanha **igual** para operacional Lab e assinante (timeline + dashboard único). Sem lista `sendIssues` no modal. Motivo: Jandira 2 / drax via layout antigo; unificar para todos.
- 2026-09-04: o relatório operacional Lab **não** lista o bloco “Erros que impactaram o envio” (`sendIssues`). Motivo: pedido do produto; Falhados no dashboard bastam. A linha do tempo do assinante permanece.
- 2026-09-04: o relatório operacional Lab exibe a mesma linha do tempo do assinante (`collectIntakeReportTimeline`). Motivo: operacionais não viam criação/atendimento/aprovação/início/fim do disparo. “Aceitas sem comprovante” continuam fora de Falhados.
- 2026-09-03: no absorb de cards do mesmo BM/WABA, usar `unionPortfolioNumbers` (por `phoneNumberId`), não `mergePortfolioNumbers`. Motivo: `mergePortfolioNumbers` é Graph-vs-cache; no absorb de duas Embedded Signup do mesmo BM apagava chips (ex.: Quantum Smart Labs). `phone_numbers` passa a paginar (`limit`+`after`). Docs: https://developers.facebook.com/docs/graph-api/reference/whats-app-business-account/phone_numbers/
- 2026-09-03: com Disparo Cloud ativo (`cloudBroadcastProtect.blockRedeploy=true` no `/health`), **não** Redeployar o `waba_disparador`. Motivo: o loop de envio vive na memória do Node; restart atrasa o lote (resume/watchdog recuperam, mas interrompem). FTP sem Redeploy e outros serviços OK. Proteção: resume no boot + watchdog ~20s + rule `waba-disparo-cloud-protect`.
- 2026-09-03: o Disparo Cloud nunca manda cabeçalho por `{ link }`. Sem arquivo local, o envio não começa; o operacional anexa a mesma foto no template aprovado. Motivo: a Graph troca o handle por URL lookaside e vários templates com a mesma imagem passam a apontar para o mesmo link morto (131053). Docs: https://developers.facebook.com/docs/whatsapp/cloud-api/reference/media/
- 2026-09-03: as 3 opções geradas pela IA no Laboratório podem ser editadas antes do envio. Motivo: o texto gerado precisava de ajuste humano; o POST à Graph usa o corpo salvo, não o rascunho original da IA. Docs: https://developers.facebook.com/docs/whatsapp/cloud-api/reference/messages
- 2026-09-03: no wizard da API Oficial o upload de leads elimina duplicidade do mesmo WhatsApp (com/sem 9º dígito) antes de contar créditos e gerar o arquivo de envio. Motivo: a planilha do assinante cobrava linha a linha; o Disparo Cloud já deduplicava só na hora do POST. A Alternativa continua por linha.
- 2026-09-03: o Disparo Cloud da Jandira 2 (`26d33b09-…`) é cancelado (void) para o Laboratório refazer o POST. Motivo: o lote 131053 ocupava o número e bloqueava o vínculo. O intake do assinante não é cancelado.
- 2026-09-03: Campanha Jandira 2 (03/09/2026, intake `368d053b-…`) fica **Em andamento** para o assinante e não fecha o relatório Meta enquanto o disparo ativo for o lote falho. Depois de um novo envio com entrega, o relatório pode fechar.
- 2026-09-03: o Disparo Cloud não envia weblink de exemplo da Graph (lookaside/fbcdn) no cabeçalho. Motivo: Campanha Jandira 2 teve 1159 falhas 131053 (HTTP 403); `wamid` não é entrega. Caminho: upload local + `{ id }`, ou abortar o lote. Docs: https://developers.facebook.com/docs/whatsapp/cloud-api/support/error-codes/ e https://developers.facebook.com/docs/whatsapp/cloud-api/reference/media/
- 2026-09-03: a linha do tempo do relatório do assinante é uma trilha de pontos — horizontal no desktop e vertical no celular. Motivo: a lista texto não mostrava a evolução. Continua omitindo marcos sem horário.
- 2026-09-03: na etapa de mídia do wizard o assinante escolhe imagem (PNG/JPG 1080×1080) ou vídeo MP4 (H.264, AAC ou sem áudio, até 16 MB). As regras aparecem antes do arquivo. Motivo: evitar upload no formato errado (MOV, WebM, 3GP, AVI). Docs: https://developers.facebook.com/docs/whatsapp/cloud-api/reference/media/
- 2026-09-03: o relatório do assinante mostra criação da campanha, início do atendimento, aprovação do template, início e fim do disparo (fuso São Paulo, dia completo + hh:mm:ss) e avisa que a Meta pode demorar até 3 horas após o fim do disparo para finalizar a coleta. Motivo: o assinante precisa ver a linha do tempo real, sem misturar aceite Graph com entrega.
- 2026-09-03: o operacional vê no relatório Lab as falhas da Meta (código + texto) e quantos envios ficaram aceitos sem webhook de entrega. Entregue só conta com `delivered`/`read`. Motivo: HTTP 200 não prova entrega; o lead `11 94949-0317` ficou `accepted` com `wamid`. Docs: https://developers.facebook.com/docs/whatsapp/cloud-api/webhooks/components/
- 2026-09-02: Campanha Jandira deste disparo (1.990 leads / 1.156 enviados / 2 falhas) tem override só na leitura: 981 entregues, 431 lidos, sem cliques. Motivo: o snapshot fechou sem delivered/read da Meta e o usuário pediu correção pontual, sem mudar a regra geral.
- 2026-09-02: Entregues/Lidos do relatório Meta vêm só do webhook `statuses`. O save do disparo faz merge com o JSON já gravado para não apagar delivered/read. Fechamento em 15 min só depois de algum entregue/lido; sem isso espera 2 h. Relatório `meta_lab` já finalizado pode ser atualizado se chegar webhook novo. Motivo: Campanha Jandira fechou 100% com 1.156 enviados e 0 entregues. Docs: https://developers.facebook.com/docs/whatsapp/cloud-api/guides/send-messages/ e https://developers.facebook.com/docs/whatsapp/cloud-api/webhooks/components/
- 2026-09-02: Disparo Cloud é menu próprio do Laboratório (`whatsapp-disparo-cloud`), acima de Automação. Motivo: o wizard não deve viver no meio de Templates; operacional antigo só vê o item depois que o master marcar no cadastro.
- 2026-09-02: o Disparo Cloud só vincula campanha do assinante **Em andamento** (`in_progress`). Motivo: Gerada ainda não está na fila operacional; o rótulo é `nome - campanha - envios`.
- 2026-09-02: número Cloud fica ocupado no início do Disparo Cloud e só libera após o relatório da campanha. Motivo: um número Ativo não pode servir dois disparos ao mesmo tempo; o envio terminar não basta, porque o Laboratório ainda coleta o relatório da Meta.
- 2026-09-02: relatório automático da Meta e card de cliques só existem para campanhas **atendidas** por quem tem Laboratório (`assignedOperacionalEmail`). Motivo: a fila sem Laboratório continua operacional/manual. O plano oficial/alternativa não dispara automação.
- 2026-09-02: destinos da planilha do disparo Cloud são normalizados para E.164 da Meta (sem `+`) antes do `POST /{PHONE_NUMBER_ID}/messages`. Motivo: Excel, máscara, DDI e falta do 9º dígito. Docs: https://developers.facebook.com/docs/whatsapp/cloud-api/reference/messages
- 2026-09-02: operacional/suporte com menu do Laboratório lê o tenant do dono (`mozart.pmo@gmail.com`). Motivo: portfólio, números e templates ficavam vazios no e-mail do operacional.
- 2026-09-02: operacional/suporte vê Laboratório pelos checkboxes do cadastro. Motivo: a allowlist Mozart zerava a seção mesmo com menus marcados (ex.: `drax@draxsistemas.com.br`). Masters que não são Mozart continuam sem a seção.
- 2026-09-02: o Arquivo da mídia do template não tem teto no Waba. Motivo: só a Meta recusa (imagem JPEG/PNG até 5 MB). O alerta mostra `error_user_msg` da Graph. Docs: https://developers.facebook.com/docs/whatsapp/cloud-api/reference/media/
- 2026-09-02: o Laboratório volta a enviar foto de perfil do chip para a Meta (`profile_picture_handle`). Motivo: o card só lia a Graph; o pedido é ditar a foto no Waba. Nome de exibição segue revisão da Meta. Docs: https://developers.facebook.com/docs/whatsapp/cloud-api/reference/business-profiles/
- 2026-09-02: **Editar perfil** só habilita no número Ativo (CONNECTED). Motivo: a Meta recusa `whatsapp_business_profile` em Pendente; o botão não deve aparecer antes do PIN.
- 2026-09-02: submit-all só trata como já enviado se o template ainda existe no banco do portfólio. Motivo: após exclusão na Meta o modal fingia sucesso sem POST Graph.
- 2026-09-02: o painel Visualizar / usar em teste e o botão Usar em teste saíram. Motivo: Visualizar já é o modal Seu modelo; o card era laboratório órfão.
- 2026-09-02: sync completo pruneia o local que a GET `message_templates` já não devolve. Motivo: exclusão no WhatsApp Manager deixava órfãos na tabela. Não prune se a paginação Graph for truncada.
- 2026-09-02: Excluir na tabela chama a Graph (hsm_id + name) e só então apaga o registro local. Motivo: a Meta tem DELETE oficial; apagar só no front deixava o template na biblioteca. Docs: https://developers.facebook.com/documentation/business-messaging/whatsapp/templates/template-management
- 2026-09-02: o prompt inclui moldes de formato da biblioteca Utility da Meta (prints). Só estrutura; o assunto é o texto base.
- 2026-09-02: a IA Utility se realimenta com templates do tenant já APPROVED+UTILITY (few-shot). Não há treino do GPT. Motivo: cada aprovação real vira referência na próxima geração.
- 2026-09-02: IA Utility inclui âncoras Confirmação, Status Confirmado, Confirmado, aprovado, concluído, atualizado, liberado. Motivo: a Meta tende a ler esses termos como mensagem de utilidade. Uma âncora por opção, sem oferta nova.
- 2026-09-02: IA Utility do laboratório Cloud usa léxico Olá / Informamos que / Para e botões informativos. Motivo: a Meta recategorizou os templates para Marketing. Docs: https://developers.facebook.com/documentation/business-messaging/whatsapp/templates/template-categorization
- 2026-09-02: card de relatórios lista todos os portfólios; o select do formulário de criação não controla a tabela.
- 2026-09-02: exclusão só no `sessionStorage` foi substituída pela Graph + prune no sync.
- Docker EasyPanel copia `dist/`. Build precisa ir no Git antes do Redeploy.
- Traefik deste VPS usa entryPoints `http`/`https`, nunca `web`/`websecure`.

## 2026-09-04 — Fan-out BM → WABAs no hydrate do portfólio

- **Decisão:** ao hidratar um card, listar todas as WABAs do Business Manager (`owned` + `client`) e unir `phone_numbers`, não só o `wabaId` da conexão.
- **Motivo:** no WhatsApp Manager um BM (ex. Quantum Smart Labs) pode ter várias WABAs; Embedded Signup grava só uma.
- **Impacto:** mais calls Graph por listagem; paginação limitada (20 páginas/edge). Sem HUP Traefik.
- **Docs:** manage-accounts Embedded Signup + Business reference Marketing API.

