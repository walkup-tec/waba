# LOG — Evidência lead 11 94949-0317 (Campanha Jandira)

## Contexto do pedido

O usuário pediu evidências do que aconteceu ao disparar para `11 94949-0317` na campanha de ontem (Campanha Jandira).

## Ações

1. Confirmado no código: a célula `11 94949-0317` normaliza para destino Meta `5511949490317`.
2. Registro individual do envio vive em `/app/data/meta-whatsapp-broadcasts.json` (campo `leads[]`: `status`, `metaStatus`, `wamid`, `error`). A planilha original/trimmed fica em `/app/data/campaign-intakes/<id>/`.
3. Este ambiente não tem `VPS_SSH_PRIVATE_KEY`. SSH direto em `72.60.51.127` recusou chave.
4. Push de trigger `.github/export-triggers/2026-09-03-jandira-lead-11949490317.json` no GitHub `master`. Workflow **Export Production Data (SSH)** run `33739772898` falhou: secret `VPS_SSH_PRIVATE_KEY` vazio no GitHub Actions (mesmo bloqueio de 18/07/2026).
5. PAT local não pode criar workflow novo (falta scope `workflow`). Sem volume `/app/data` não há evidência runtime deste número.

## Como validar (no VPS, somente leitura)

```bash
CONTAINER="$(docker ps --format '{{.Names}}' | grep -E 'waba.*disparador' | grep -vE 'v02|v01' | head -1)"
docker exec "$CONTAINER" node -e '
const fs=require("fs"); const p="/app/data/meta-whatsapp-broadcasts.json";
const needles=["5511949490317","11949490317","949490317"];
const hit=s=>{const d=String(s||"").replace(/\D/g,""); return needles.some(n=>d===n||d.endsWith(n)||n.endsWith(d));};
const raw=JSON.parse(fs.readFileSync(p,"utf8"));
for (const c of raw.campaigns||[]) {
  const leads=(c.leads||[]).filter(l=>hit(l.waId)||hit(l.numero));
  const name=(c.intakeCampaignId||"")+" "+(c.templateName||"");
  if(!leads.length && !/jandira/i.test(JSON.stringify(c))) continue;
  if(leads.length) console.log(JSON.stringify({id:c.id,intake:c.intakeCampaignId,template:c.templateName,status:c.status,sent:c.sent,failed:c.failed,skipped:c.skipped,leads},null,2));
}
'
```

Sem `sendText`. Sem alterar JSON.

## Segurança

Nenhum token impresso. Export completo de `/app/data` não chegou a este workspace.

## Palavras-chave

`jandira`, `94949-0317`, `5511949490317`, `meta-whatsapp-broadcasts`, `evidencia`, `VPS_SSH_PRIVATE_KEY`
