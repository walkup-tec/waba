# LOG — VPS scripts contingência atualizados

## Resultado
Usuário atualizou `/root` com SHA `01f29d7`. Grep só achou comentário anti-overlay (`waba_paginadevendas:3000`) — esperado: v2 usa `f"http://{gw}:30210/"`, não literal `172.17.0.1` no arquivo.

## Status
Plano gravado no VPS OK. Pendência opcional: Cloudflare Always Online.
