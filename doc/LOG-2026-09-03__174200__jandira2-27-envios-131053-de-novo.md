# LOG — Jandira 2 retrabalho: 27 skipped + 131053 de novo

## Evidência do VPS (usuário)

Broadcast novo `c8e99348-4579-476c-b52d-af4f05d509df` (17:32 UTC, `running`):

- total 1162, sent 179, failed 0, **skipped 27**
- wamid 180, delivered 0
- metaStatus: failed 180, queued 982
- errorCode: **131053 × 180**

Lote velho `26d33b09-…` continua void, 1159 × 131053, skipped 27, delivered 0.

## Conclusão

Os **27** que o painel mostrou não foram entregas. São os **27 inválidos** (`skipped`) — a mesma conta do lote anterior — e/ou os primeiros aceites Graph, todos recusados no webhook. Ninguém recebeu.

O retrabalho está **ainda enviando**. Cada POST novo tende a repetir 131053.

## Correção no código

- Abortar o lote no primeiro 131053 sem entrega (`shouldAbortBroadcastOnHeaderMediaFailure`).
- Void do `c8e99348-…` no boot.
- O loop do disparo relê o JSON e para se o lote foi cancelado.

No VPS o operador já rodou void + `docker restart` (2026-09-03):

```
voided c8e99348-4579-476c-b52d-af4f05d509df
waba_waba_disparador.1.ba4xrubgifa1yb817yiuvigi0
```

Depois do restart: HTTPS `/health` **200**, marker `DEPLOY-2026-09-03-171800-oficial-dedupe-ai-edit`, intake API 6. O loop em memória morre no restart; `voidedAt` no JSON impede ocupação/relatório desse lote.

## Palavras-chave

jandira 2, 131053, skipped 27, c8e99348, abort
