# LOG — Cluster hex único (imagem composta)

## Contexto

Usuário enviou PNG único com os 3 hexágonos já posicionados (layout zig-zag do mock). Substituir as 3 imagens separadas na coluna esquerda da tela Contratar.

## Solução

- Asset: `media/disparos-hex-cluster.png` (1024×1024).
- HTML: um `<img class="disparos-hex-cluster-art">` no lugar dos 3 `disparos-hex-art`.
- CSS simplificado: largura responsiva (320px base, 380px ≥1100px).

## Arquivos

- `media/disparos-hex-cluster.png`, `index.html`, `dist/*`

## Validar

Créditos → Contratar: arte composta à esquerda, listas à direita.

## Palavras-chave

`disparos-hex-cluster.png`, `disparos-hex-cluster-art`
