# LOG — Rule Cursor "sobe para o servidor"

## Contexto

Regra de projeto para, ao dizer "sobe para o servidor", o agente executar commit + push em `master` e disparar deploy FTP via GitHub Actions.

## Arquivo criado

- `.cursor/rules/sobe-para-o-servidor.mdc` (`alwaysApply: true`)

## Palavras-chave

`sobe para o servidor`, `deploy FTP`, `GitHub Actions`, `master`
