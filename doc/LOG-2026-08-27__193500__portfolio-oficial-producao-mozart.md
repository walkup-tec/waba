# LOG — Portfólio oficial em produção para Mozart

## Contexto

Subir o fluxo novo (card de portfólio, lista de números, PIN/ativar) para produção, mantendo o Laboratório só em `mozart.pmo@gmail.com`.

## Solução

- `metaOfficialPortfolioLab` ligado por padrão (override `WABA_META_OFFICIAL_PORTFOLIO_LAB=0` se precisar desligar).
- Menu Laboratório em produção: allowlist Mozart. Walkup e quantumivst não veem.
- V02 local (`/version-02`) continua podendo validar com qualquer login.
